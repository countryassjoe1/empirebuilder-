# Austin Empire — Skeleton

This repository is a starter skeleton for the Austin Empire project.

Quick start:

1. Review `docs/overview.md` and update `docs/public-codex.json`.
2. Fill the `src/` folders with content and implementations.
3. Use `scripts/append-log.sh` to add build notes.

Development:
- `npm install` for tooling (if needed)
- CI is configured in `.github/workflows/build.yml`
